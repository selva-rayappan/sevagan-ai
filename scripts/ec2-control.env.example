# Local config for scripts/ec2-start.sh and scripts/ec2-stop.sh.
# Copy to scripts/ec2-control.env (gitignored) and fill in real values.
# Requires the AWS CLI installed and configured (`aws configure`) with
# credentials that can call ec2:DescribeInstances/StartInstances/StopInstances
# on this instance.

INSTANCE_ID=i-xxxxxxxxxxxxxxxxx
AWS_REGION=us-east-1
EC2_HOST=54.208.201.48
EC2_USER=ubuntu
SSH_KEY_PATH=/c/Users/selvakumar.rayappan/downloads/sevagan-prod-key.pem
